# ClusterK-Means

An implementation of the K-Means unsupervised clustering algorithm written in C. This framework partitions point clusters by executing coordinate geometric recalculations iteratively.

## 🚀 Build Instructions
```bash
gcc main.c cluster.c -o cluster_engine -lm
./cluster_engine
